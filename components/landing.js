import React, { useState, useEffect } from "react";
import { Animated, Text, View } from "react-native";

export default function Landing() {
  useEffect(() => {
    setTimeout(() => {
      s;
    }, 2000);
  }, []);

  return (
    <View style={{ backgroundColor: "red", flex: 1 }}>
      <Text>Sustainable Foodie</Text>
    </View>
  );
}
